
// This mock API service is no longer actively used as the application
// has been migrated to use Supabase via services/apiService.ts.
// Exporting an empty object or minimal structure to prevent build errors
// if any file still accidentally imports it, though direct imports should be removed.

export const mockApiService = {};
